# Images

- `favicon.svg` — site favicon
- `placeholder.svg` — fallback for profile and about images

For production, add your own images (e.g. `killmonger.jpg`, `bill.gif`) and update the Gallery in `src/pages/GalleryPage.jsx` to use `/images/yourfile.jpg` instead of picsum.photos URLs.
